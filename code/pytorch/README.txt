
There are two runnable files in this folder:

train_predictor.py 

runs the prediction algorithm using command line argument to select the hyperparameters. Use the -h flags to see the options.

infiller.py

runs the code to remove a digit from an image. It will prompt the user for test image number and will process the infile.
